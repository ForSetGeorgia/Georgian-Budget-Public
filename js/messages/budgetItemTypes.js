const { defineMessages } = require('react-intl')

const messages = defineMessages({
  program: {
    one: {
      id: 'budgetType.program.one',
      description: 'Singular program text',
      defaultMessage: 'Program'
    }
  }
})

module.exports = messages
